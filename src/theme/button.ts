import type  { ComponentStyleConfig } from "@chakra-ui/theme"

const Button: ComponentStyleConfig =  {
    variants: {
    },
    defaultProps: {
        colorScheme: 'teal'
    }
}


export { Button }